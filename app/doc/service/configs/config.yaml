server:
  grpc:
    addr: "${GADDR:0.0.0.0:18080}"
    # timeout: "${GTIMEHOUT:1s}"

app:
  name: doc
  version: v1.0.0
  env: "${ENV:dev}"
  log:
    level: "${LOG_LEVEL:-1}"
    filename: "${LOG_FILENAME:doc.log}"
    max_size: "${LOG_MAX_SIZE:20}"
    max_age: "${LOG_MAX_AGE:30}"
    max_backups: "${LOG_MAX_BACKUPS:10}"
